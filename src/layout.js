/**
 * The strip along the top of the canvas that the view controls sit in.
 *
 * The detail panel starts below it rather than over it: a control that disappears the moment you
 * select something is not a control, and moving it into a panel only traded one wrong place for
 * another. The change list is on the other side and clears nothing, so it stays at the top.
 */
export const CONTROLS_ROW_HEIGHT = 46;
